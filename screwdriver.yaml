workflow:
    - echostuff

shared:
    image: node:6

jobs:
    main:
        steps:
            - install: npm install
            - test: npm test

    echostuff:
        steps:
            - echo: echo "hello"
